[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Google ADK for AI agent framework
google-adk = ">=1.19.0"

# Data processing
pandas = ">=2.0.0"

# Environment variable management
python-dotenv = ">=1.0.0"

# FastAPI web framework
fastapi = ">=0.104.0"

# ASGI server with standard extras (for better performance)
uvicorn = {extras = ["standard"], version = ">=0.24.0"}

# Data validation
pydantic = ">=2.0.0"

[dev-packages]
# Testing framework
pytest = "*"
pytest-asyncio = "*"

# Code formatting
black = "*"

# Linting
flake8 = "*"

# Type checking
mypy = "*"

[requires]
# Supports Python 3.10+ (currently using 3.13 in development)
python_version = "3.10"
